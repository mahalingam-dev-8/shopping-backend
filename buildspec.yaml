version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - npm install --legacy-peer-deps
  build:
    commands:
      - npm run prisma generate
      - npm run build
artifacts:
  files:
    - '**/*'
